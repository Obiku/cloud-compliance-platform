# Phase 6 — Control Testing Workpaper

Test date for all procedures below: **2026-06-20**, unless noted otherwise.

| Control ID | Test date | Procedure | Sample/population | Result | Evidence reference | Notes |
|---|---|---|---|---|---|---|
| `CTRL-IAM-NOADMIN` | 2026-06-20 | Re-performance: re-seeded the Phase 1 issue (`terraform apply -target=module.iam.aws_iam_user_policy_attachment.seeded_legacy_admin[0]`, completed 14:30:52Z), then measured detection time across all three layers | Population: 1 (the seeded resource) | **Pass** — detected by all three layers. AWS Config (configuration-change-triggered, not forced): 14:32:19Z (**87s**). Security Hub (same underlying check): 14:32:33Z (**101s**). Phase 4 Lambda (on-demand invoke, not the daily schedule): re-flagged within its ~13s execution, invoked at 14:31:03Z | S3 `iam-governance/2026/06/20/143115/report.md`; Config rule `iam-user-no-policies-check-conformance-pack-krtldhrkw`; Security Hub finding "IAM users should not have IAM policies attached" | Initially attempted forcing evaluation on the wrong Config rule (`iam-policy-no-statements-with-admin-access...`, which checks policy *documents* for admin statements, not user attachments) — corrected to `iam-user-no-policies-check...`, which evaluates `AWS::IAM::User`. Config/Security Hub detection happened organically before the forced evaluation was even needed. Production detection latency for the *daily schedule* (vs. this on-demand test) is bounded by the 24h EventBridge interval, not the ~13s shown here. |
| `CTRL-IAM-MFA` | 2026-06-20 | 100% population test: Phase 4 scanner enumerates every IAM user in the account via pagination (no sampling) | Population: all IAM users in the account (9 at time of test) | **Pass** (control functioning correctly) — 4 console users without MFA correctly identified (`admin-charlie`, `analyst-diana`, `dev-alice`, `dev-bob`), all correctly marked out-of-scope for this project's remediation (belong to `access-review-prod`) | S3 `iam-governance/2026/06/20/143115/report.md` | Confirms `scanner.py`'s `list_users` paginator covers the full population, not a sample — also unit-tested in `test_scanner.py`. |
| `CTRL-IAM-ACCESSKEY-AGE` | 2026-06-20 | 100% population test: same scanner run, all access keys across all users | Population: all active access keys in the account | **Pass** — 0 findings (no active keys older than 90 days at test time) | S3 `iam-governance/2026/06/20/143115/report.md` | Absence of findings confirmed by re-reading the report, not assumed from a summary count. |
| `CTRL-IAM-UNUSED-ROLES` | 2026-06-20 | 100% population test: same scanner run, all IAM roles excluding service-linked roles | Population: all non-service-linked IAM roles in the account | **Pass** — 0 findings (no roles unused 90+ days at test time) | S3 `iam-governance/2026/06/20/143115/report.md` | — |
| `CTRL-S3-ENCRYPTION` | 2026-06-20 | 100% population test: `aws s3api get-bucket-encryption` against every bucket this project owns | Population: 4 (`cloudtrail-logs`, `evidence`, `legacy-data`, `terraform-state`) | **Pass** — all 4 encrypted (3× AES256, 1× SSE-KMS with a customer-managed key for the evidence bucket) | Raw CLI output captured in this session; cross-checked against Security Hub's own encryption-related findings (none FAILED for these 4 buckets) | The 4 other buckets in the account (`access-review-prod-*`, `access-review-tfstate-*`, `aws-access-review-reportbucket-*`) belong to the other project and are out of scope for this control's ownership. |
| `CTRL-CONFIG-NISTCSF` | 2026-06-20 | Re-performance (shared with `CTRL-IAM-NOADMIN` above — the same re-seed event is the test trigger for this control too, since it's the same underlying Config rule evaluation) | Population: 1 (the seeded resource) | **Pass** — see `CTRL-IAM-NOADMIN` row for timing | Same as above | — |
| `CTRL-SECURITYHUB-FINDINGS` | 2026-06-20 | Re-performance (shared with `CTRL-IAM-NOADMIN` above) | Population: 1 (the seeded resource) | **Pass** — see `CTRL-IAM-NOADMIN` row for timing | Same as above | — |
| `CTRL-CLOUDTRAIL-LOGGING` | 2026-06-20 | Inspection: re-ran the Phase 5 evidence collector and reviewed the snapshot | Population: 1 trail (`cloud-compliance-platform-trail`) | **Pass** — confirmed multi-region, `IsLogging: true`, recent delivery timestamp, 50-event sample present | S3 `evidence/CTRL-CLOUDTRAIL-LOGGING/2026/06/20/135922/snapshot.json` | — |
| `CTRL-IAM-GROUP-MFA` | 2026-06-20 | Inspection: manually reviewed `terraform/modules/iam/main.tf`'s `require_mfa` policy document against the AWS-recommended "deny unless MFA" template | Population: 3 (the three RBAC groups, all reviewed — small population, no sampling needed) | **Pass** (by inspection only — see note) | `terraform/modules/iam/main.tf` lines defining `data.aws_iam_policy_document.require_mfa` | **No tool can continuously test this control's actual effect** (see `docs/rcm.md` notes and VL-004) — inspection of the policy logic is the only test performed. A true re-performance test would require provisioning a non-MFA'd test principal and attempting a denied action; out of scope for this phase. |
| `CTRL-CICD-POLICYGATE` | 2026-06-20 | Inspection: reviewed GitHub Actions run history for evidence the gate has actually blocked real failures, not just passed vacuously | Population: all pipeline runs to date (sampled the ones with non-trivial outcomes) | **Pass** — the gate has genuinely failed real runs and required real fixes: Phase 2's first run failed on a Trivy action version typo and a Terraform backend/credential issue (3 fix-forward commits before green); Phase 4's first run failed on an `Invalid count argument` Terraform error caught before merge | `.github/workflows/compliance-pipeline.yml` run history (commits `6e31769`–`8e891e2`) | This is meaningfully different from "the pipeline has never failed" — it failed for real reasons multiple times and was fixed each time, which is closer to genuine evidence the gate works than a clean run history would be. |
| Exception log review | 2026-06-20 | Inspection: reviewed every entry in `docs/vulnerability_log.md` for owner, justification, and remediation/retest plan | Population: 7 entries (all reviewed — small population) | **Pass with 3 exceptions logged** — VL-001/002/003/004 are correctly classified as not-real-gaps/intentional/false-positive with adequate justification; VL-005/006/007 were genuine accepted-risk backlog items **without a formal retest date**, which this review treated as a finding | `docs/vulnerability_log.md` | Logged as ServiceNow Issues `IPT0020003` (VL-005), `IPT0020004` (VL-006), `IPT0020005` (VL-007), each with owner `obiku` and retest date 2026-09-30. `docs/vulnerability_log.md` updated with the issue references. |

## Summary

11 control tests performed across 9 distinct controls (2 controls share the same
re-performance event). All passed. One control (`CTRL-IAM-GROUP-MFA`) has a
documented testing limitation rather than a failure — flagged, not hidden. Three
process exceptions found during the exception-log review were closed by logging
them as real ServiceNow Issues with owners and retest dates, satisfying the
project plan's requirement to log exceptions rather than leave them as an
undated backlog note.

**Post-test cleanup:** the `CTRL-IAM-NOADMIN` re-performance test left
`AdministratorAccess` genuinely re-attached to `legacy-admin` (that was the point
of the test). After documenting the result, ran
`python -m phase4_iam_governance.cli remediate --check iam_admin_access_attached
--resource-id cloud-compliance-platform-legacy-admin --approve` again to restore
the secure baseline. The Terraform code still defines the attachment (so this is
expected to drift again — `terraform plan` will show 1 resource to add until the
next time this control needs re-testing).
